# Server configuration
# Copy to .env and adjust values as needed

# Network
PORT=3000
HOST=0.0.0.0

# Database
# File path for the SQLite database. Use ':memory:' for ephemeral in-memory (testing only).
DB_PATH=fitness.db

